Quick start

1. Install dependencies:
   npm install

2. Start the dev server:
   npm run start

3. Open iOS simulator or Expo Go on your device.

Notes:
- This scaffold uses React Native Paper for polished components and a shared theme.
- Update `API_BASE` in `mobile/api/apiClient.js` to point to your backend.
