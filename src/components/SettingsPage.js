import React from 'react';

const SettingsPage = () => (
  <div className="container">
    <h1>Settings</h1>
    <p>Account, team, security, integrations.</p>
  </div>
);

export default SettingsPage;
